// src/proxy.js (migrated from middleware.js for Next.js 16+)
// Middleware is incompatible with `output: export`. This file is now disabled.
// If you need middleware functionality, consider using a server-side rendering setup instead.

export default function proxy() {
  console.warn('Middleware functionality is disabled due to static export configuration.');
}
